import { InjectionToken } from '@angular/core';

export const PE_HEADERMENU_DATA = new InjectionToken<any>('PE_HEADERMENU_DATA');
export const PE_HEADERMENU_THEME = new InjectionToken<any>('PE_HEADERMENU_THEME');
