export interface MenuCardLink {
  name: string;
  url: string;
  nameButton?: string;
  disabled?: boolean;
  active?: boolean;
  isHovered?: boolean;
  isButton?: boolean;
  key?: string;
  icon?: string;
  loading?: boolean;
}
