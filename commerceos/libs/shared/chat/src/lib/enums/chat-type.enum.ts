export enum PeMessageChannelType {
  Integration = 'integration',
  Private = 'private',
  Public = 'public',
}

export enum PeMessageChannelRoles {
  Admin = 'admin',
  Member = 'member',
  Subscriber = 'subscriber',
}

export enum PeMessageConversationMemberAddMethod {
  Include = 'include',
  Owner = 'owner',
}

export enum PeMessageChatSteep {
  Contacts = 'contacts',
  Main = 'main',
}

export enum PeMessageChannelSteep {
  Additional = 'additional',
  Main = 'main',
  Type = 'type',
}

export enum PeMessageChannelMode {
  Admin = 'admin',
  Common = 'common',
  Subscriber = 'subscriber',
}

export enum PeMessageGroupSteep {
  Contacts = 'contacts',
  Main = 'main',
}
