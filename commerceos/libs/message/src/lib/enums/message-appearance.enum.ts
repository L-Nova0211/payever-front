export enum PeMessageColorTabs {
  AccentColor = 'Accent Color',
  Background = 'Background',
  Message = 'My message'
}

export enum PeMessageBubbleLayouts {
  Logo = 'simple',
  LogoText = 'logo_text',
  Text = 'text'
}

export enum PeMessageBubbleStyle {
  Circle = 'circle',
  Rounded = 'rounded',
  Sharp = 'sharp'
}

export enum PeMessageBubbleBrand {
  Payever = 'payever',
  Custom = 'custom'
}
