import { PebEditorTabComponent } from './tab.component';

describe('PebEditorTabComponent', () => {

  const component = new PebEditorTabComponent();

  it('should be defined', () => {

    expect(component).toBeDefined();

  });

});
