import { PebRendererGridCellSlot } from './grid-cell.slot';

describe('PebRendererGridCellSlot', () => {

  const slot = new PebRendererGridCellSlot({} as any);

  it('should be defined', () => {

    expect(slot).toBeDefined();

  });

});

