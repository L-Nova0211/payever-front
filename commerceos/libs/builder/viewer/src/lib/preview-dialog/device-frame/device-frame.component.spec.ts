import { PebViewerDeviceFrameComponent } from './device-frame.component';

describe('PebViewerDeviceFrameComponent', () => {

  const component = new PebViewerDeviceFrameComponent();

  it('should be defined', () => {

    expect(component).toBeDefined();

  });

});
